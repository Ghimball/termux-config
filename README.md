### Termux Config

```bash
cd $HOME &&
pkg upgrade -y &&
pkg update -y &&
pkg install git &&
git clone https://github.com/Ghimball/ghimball.git &&
cd termux-config &&
sh ./install.sh
```

### ZSH Setup
```bash
sh zsh-setup.sh
```
